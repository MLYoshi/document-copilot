# --- Database (SQLAlchemy + Alembic) ---

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/document_copilot

# --- JWT Auth ---

# Generate with: openssl rand -hex 32 (minimum 32 bytes, enforced at startup)
JWT_SECRET_KEY=change-me-change-me-change-me-change-me
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# --- OpenRouter (needed once the LLM / embedding layer is wired up) ---

OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
EMBEDDING_MODEL=baai/bge-m3
EMBEDDING_DIMENSIONS=1024
# Chat model for the assistant agent (OpenRouter model slug)
CHAT_MODEL=minimax/minimax-m3:free
# Number of retrieved passages injected into the prompt (must be >= 1)
RETRIEVAL_TOP_K=8

# --- Server ---

# Comma-separated browser origins allowed to call the API (CORS).
ALLOWED_ORIGINS=http://localhost:5173
